/**
 * Environment (dev par défaut)
 * - Contient la configuration Firebase + API backend
 */

export const environment = {
    production: false,
    firebaseConfig: {
    // Copier et coller les config du projet Firebase Ici ! 
   }
  };